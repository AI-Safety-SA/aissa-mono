# All Community Members to Edit Own Records

Each community member enters their name and their email address.
We try for exact email match, then exact name match, then fuzzy match. We display all options (?) and they select. If they have a placeholder, they have to provide a real email, confirm the real email *{ (how do we authorise these users? You can literally impersonate anyone like this... also will have to configure mailgun or something like that). }*. 

> We need some mechanism for these adjustments to be stored in a "staging" state and they need to be reviewed by a team member before updating real records.

The community member then gets to update their own personal profile with all the information we have fields for (maybe we want to add a few more fields, like cv upload, that is more relevant for job seekers). Next page, the community member gets to select from events they attended (optionally adding a rating *{or testimonial – unsure if we should hide this complexity and ask for a general testimonial once at the end, or allow per engagement testimonials like the db supports. UX is important here and I'm leaning toward less typing }*). Same flow for programs. Cohorts...no *{ Cohorts is a pain in the ass because I don't actually like how they are done. They feel way too separate from Programs. This is a concern for another time. }* For now, keep as is, and allow people to simply select all the programs that they engaged with. We allow everyone to select their engagement type and will review this manually. Programs also ask for rating and testimonial *{ if you completed, attended, facilitated or spoke at the event? – unsure about conditionally allowing this. }*. The engagement dates are not shown and are auto-filled based on the event and program info. 

This doesn't address creating 'engagement impacts' though, unless we specifically ask for every event if this impacted a career decision (boolean check). If yes, give text prompt asking for an explanation. 

Once they have submitted the updated results, they need to get saved in a temporary store, and an admin has to go and review each person's additions. This should probably be displayed on a per-person basis. And we can see all the fields that will be created, updated or deleted (person didn't attend this, wasn't involved here). Admins would need to be able to scroll through and *{ select a sub-set to }* approve or deny.  